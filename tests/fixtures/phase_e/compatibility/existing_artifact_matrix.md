# Frozen existing artifact compatibility matrix

The closed historical file list and its SHA-256 values are in
`existing_artifact_fixture_inventory.schema1.json`.  Each item remains a
Phase-A through Phase-D compatibility input and is read through its existing
public reader contract; Phase E adds no migration or rewrite.
